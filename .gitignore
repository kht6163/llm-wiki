# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# Project data / secrets
.env
!.env.example
*.db
*.db-wal
*.db-shm

# Default local vault & data dirs (override via .env)
/vault/
/data/

# HuggingFace model cache (if redirected into the project)
.cache/

# Local agent / editor config (kept locally, not committed)
/AGENTS.md
/CLAUDE.md
/.claude/
# Design context docs (kept locally, not committed)
/PRODUCT.md
/DESIGN.md
/.impeccable/

# coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml

# Isolated Codex/Superpowers worktrees
/.worktrees/
/.superpowers/
/docs/superpowers/
/playwright-report/
/test-results/
